<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="rnd.project.MainActivity">

    <Button
        android:id="@+id/invoerButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="+ Nieuwe Invoer +"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:onClick="gotoNieuweInvoer" />

    <Button
        android:id="@+id/settingsButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/invoerButton"
        android:layout_alignParentEnd="true"
        android:text="Settings"
        android:onClick="gotoSettings" />

    <Button
        android:id="@+id/lijstButton"
        android:layout_width="match_parent"
        android:layout_height="180dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:onClick="gotoLijst"/>

    <Button
        android:id="@+id/uitgavenButton"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:layout_alignParentEnd="true"
        android:layout_below="@+id/invoerButton"
        android:background="match_parent"
        android:onClick="gotoUitgaven"/>

    <Button
        android:id="@+id/inkomstenButton"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:layout_below="@+id/invoerButton"
        android:background="match_parent"
        android:onClick="gotoInkomsten"/>

    <ScrollView
        android:id="@+id/maandenScroll"
        android:layout_width="80dp"
        android:layout_height="50dp"
        android:layout_above="@+id/inkomstenButton"
        android:layout_alignParentStart="true"
        android:fillViewport="true"
        android:scrollbarStyle="insideOverlay">
    </ScrollView>

</RelativeLayout>
